import React from "react";
import { AuroraHero } from "../Hero/AuroraHero";

export const HomePage = (): React.ReactNode => {
  return (
    <main className="mt-0">
      <AuroraHero />
    </main>
  );
};
